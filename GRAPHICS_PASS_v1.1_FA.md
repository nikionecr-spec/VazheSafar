# واژه‌سفر ۱٫۱ — گزارش پاسِ گرافیکی و صوتی

> این پاس در پاسخ به فیدبک‌هایت انجام شد: «کاراکتر نشسته نیست»، «گربه روی هواست»، «پایین صفحه خالی»، «همه چیز ریز است»، «کارتونی نباشه»، «پروفایل کار نمی‌کند»، «چرخ/کلمات تراز نیست»، «صدای سنتور عوض شود»، «هر بخش موسیقی خودش»، «روان‌شناسی اعتیاد»، «چیدمان رقیب‌ها».

## ۱) چه چیزهایی عوض شد

### الف) کاراکترها (گربه روی هوا نبود، حالا نشسته)
- آرت جدید ساخته شد: `assets/art/char_seated.png` (پیرمرد واقعاً **نشسته روی صندلی، پشت میز**، با دست‌های روی میز) و `assets/art/cat.png` (بچه‌گربه نشسته‌ی مودب روی میز).
- هر دو با کلیدرنگی ساخته و آلفای تمیز شدند (حاشیهٔ سبز، بدون هاله).
- زیر هر دو **سایهٔ تماس (Contact shadow)** گرافیکی گذاشته شد تا حس وزن و نشستن بدهند.
- پیرمرد لایه‌به‌لایه پشت میز قاب می‌شود و گربه روی سطح میز کنارش.

### ب) چیدمان صفحهٔ اصلی (پایینِ خالی و دکمه‌های ریز → حل شد)
- HUD بالا: کارت بازیکن (آواتار طلایی + نام + نوار XP + نشان سطح)، قرص سکهٔ شیشه‌ای با دکمهٔ **+**، دکمهٔ تنظیمات.
- ریل کناری با **۳ آیکون درشت ۱۵۰px** (فروشگاه، کتابخانه، تم‌ها) — دیگر ریز نیست.
- **کارت «مرحلهٔ بعدی»** با شمارهٔ درشت، نام دنیا، ستاره‌ها و نوار پیشرفت دنیا (هدفِ نزدیک و همیشه قابل‌دسترس).
- آیکون درشت **جایزهٔ روزانه** روی میز.
- **CTA تمام‌عرض ۸۴۴×۱۸۶** با زیرنویسِ مرحله، هالهٔ نور، پالس و درخشش عبوری.
- **نوار پایین ۵ تبی** با آیکون‌های درشت و تبِ فعالِ برجسته — دیگر پایین صفحه خالی نیست.

### ج) زیبایی‌شناسی: «گران و نرم» به‌جای تخت و کارتونی
- کتابخانهٔ آرت پروسیدورال `scripts/Art.gd`: پنل‌های گرادیانی با لبهٔ سه‌بعدی، گلاس نرم، سایهٔ داخلی، حلقهٔ طلایی، سایهٔ زمینی، دیسک نور، ریبون، نوار پیشرفت، کاشی حرف، برچسب اعلان.
  (این‌ها تصویر واقعی می‌سازند، نه StyleBox تخت؛ اندازهٔ کوچک ساخته و با فیلتر خطی بزرگ می‌شوند.)
- دکمه‌های Juicy دوباره نوشته شدند: بدنهٔ گرادیانی + لبهٔ پایین ضخیم + گلاس شیشه‌ای + درخشش عبوری + squash/bounce.
- `UIKit.gd` نسخهٔ ۲: قرص شیشه‌ای، کارت بازیکن، نوار پایین، سرتیتر بخش، کاشی آمار.

### د) پروفایل (کار نمی‌کرد → حالا صفحهٔ کامل است + تب در نوار پایین)
- کارت قهرمان با آواتار، سطح، نوار XP، **ویرایش نام** (دیالوگ گرافیکی).
- **۶ کاشی آمار**: کلمات، کلمات جایزه‌ای، راهنما، مراحل، ستاره‌ها، روزهای پیاپی.
- **۶ دستاورد** با قفل/بازشدن (اولین قدم، واژه‌شناس، ستاره‌چین، جایزه‌جو، جهان‌گرد، بی‌نیاز).
- از هم توی نوار پایین و هم با کلیک روی HUD صفحهٔ اصلی باز می‌شود.

### ه) بقیهٔ صفحه‌ها از صفر بازچینش شدند
- **فروشگاه:** کارت هدیهٔ روزانه (دکمهٔ درشت تمام‌عرض)، شبکهٔ ۲×۲ بسته‌ها با برچسبِ برندهٔ مسابقهٔ قیمت، بسته‌های جم، کارت راهنما.
- **ماموریت‌ها:** کارت جایزهٔ روزانه با **نوار ۷ روز** (روزهای روشن/امروز/آینده)، سه کارت ماموریت با نوار پیشرفت و دکمهٔ درشت، شبکهٔ آمار.
- **تم‌ها، کتابخانه، تنظیمات:** کارت‌های درشت، پیش‌نمایش رنگی تم‌ها، جستجوی واژه + چیپ‌های رنگی، کلیدهای کشوییِ گرافیکی و دیالوگ تأیید بازنشانی.
- **گیم‌پلی:** کاشی حروف بزرگ‌تر (۸۸×۱۰۴)، تابلو و چرخِ چوبیِ گرافیکی (پروسیدورال)، **پیش‌نمایش کلمه در مرکز چرخ**، برچسب «چند از چند کلمه»، دکمه‌های درشت بُر زدن/راهنما، پاپ‌آپ پایان مرحله با ستاره‌های جهنده و پرواز سکه.
- **نقشه:** گره‌های ۱۵۲px، هالهٔ گرهٔ فعلی، هدر شیشه‌ای با صفحه‌بندی دنیا، نوار پایین.

---

## ۲) تحقیق: رقبا چه می‌کنند (و چه چیزی را برداشتم)

| رقیب | نکتهٔ کلیدی |
|---|---|
| **آمیرزا** (پرشمارترین کلمه‌بازی ایران) | موسیقی بی‌کلامِ دلنشین که کاربر **در منو می‌ایستد تا گوش بدهد**؛ رابط کاربری با رنگ‌بندی گرم و بومی؛ مرحله‌های چندکلمه‌ای با سلسله‌مراتب رشد |
| **Word Cookies / Mango** | کاشی‌های حروفِ برجستهٔ واقع‌گرا با کشیدن انگشت؛ پاداش‌های متغیر و رویدادهای فصلی؛ جستجو برای کلمه‌های اضافه |
| **Wordscapes** (گیم‌فیل و «خلسه») | بازخورد لمسی/صوتی نرم، حرکت تدریجی، مسیر پیشرفت بی‌پایان |
| **مرورهای کاربران** | «موسیقی بعد از مدتی حوصله‌سربر می‌شود» و «دکمهٔ پالس‌زنندهٔ مأموریت آزاردهنده است» → یعنی موسیقی باید **تغییر کند** و پالس‌ها **ملایم** باشند |

نتیجه‌گیری‌های پیاده‌شده: موسیقیِ مخصوص هر بخش + ترکیب دو-ماژورِ بی‌تنش + پالس ملایم و نه پرخاشگر.

---

## ۳) صدا: چرا سنتور رفت و چه آمد

با ده‌ها نمونه‌ی UI بازی‌های کژوال و مراجع صداسازی بازی بررسی شد؛ جمع‌بندی:
- جلوه‌های رابط باید **۱۰۰ تا ۳۰۰ میلی‌ثانیه**، کوتاه، «لطیف و مخملی» باشند، با **تغییرات ریز تصادفی** در هر پخش تا خسته‌کننده نشوند.
- تیمبر برنده در بازی‌های کلمه‌ای مدرن: **کالیمبا/ماریمبا و زنگ‌های سینوسی نرم** (نه سیم مضرابیِ تیز مثل تار/سنتور).
- مدیریت طیف: خطا نباید تیز باشد، بلکه «کوفته و کم»؛ موفقیت آرپژ گرم؛ سکه/ستاره زنگ روشن.

پیاده‌سازی (`scripts/Audio.gd` نسخهٔ ۲):
- `_mallet()` = سینوس + هارمونیک‌های ملایم + پاکت نمایی + فضای کوتاه اتاقِ چوبی → تیمبر نرمِ کالیمبا.
- `_felt_tap()` = نویز فیلترشده + بدنهٔ کم‌فرکانس برای دکمه‌ها (حس «لمسِ مخملی»).
- جلوگیری از خستگی شنیداری: تغییر pitch تصادفی ‎±۱٫۵٪‎ روی هر پخش.
- **دوکِ خودکار**: وقتی کلمهٔ درست/جایزه/برد پخش می‌شود، موسیقی لحظه‌ای ۶٫۵ دسیبل کم می‌شود.
- **موسیقی هر بخش جدا:** `home` (۸۴ bpm، شاداب)، `map` (۷۲ bpm، سفرآمیز)، `level` (۶۸ bpm، تمرکزی و خالی)، `shop` (۹۶ bpm، پرانرژی)، `quests` (۸۰ bpm، امیدوارانه) — هر کدام پیشروی آکورد + آرپژ + پد + ضربهٔ دَفِ محو، با کراس‌فید نرم.
- فایل‌های WAV آمادهٔ گوش دادن در `audio_preview/` (اگر خواستی جدا بشنوی).

---

## ۴) روان‌شناسی: نقشهٔ «برگشتن دوباره» (Hooked Loop)

بر پایهٔ منابع بررسی‌شده (اثر زایگارنیک، پاداش متغیر، streak، اثر گلایدری):

| اصل | پیاده‌سازی در بازی |
|---|---|
| **پاداش متغیر** (نه ثابت) | کلمات جایزه‌ای، سرنخ‌های رنگی، ستاره‌های پله‌ای |
| **اثر زایگارنیک** (کار ناتمام کشش می‌سازد) | کارت «مرحلهٔ بعدی» روی خانه + نوار دنیا «۳ از ۱۰» + ماموریت‌های همیشه در جریان |
| **streak و ترس از شکستن زنجیر** | نوار ۷ روزِ جایزهٔ روزانه با نشانگر «امروز» |
| **هدفِ نزدیک و همیشه‌قابل‌دسترس** | دکمهٔ CTA با زیرنویس مرحله + گرهٔ درخشان روی نقشه |
| **جمع‌آوری و نمایش پیشرفت** | کتابخانهٔ واژه‌ها (چیپ‌های سبز/طلایی)، ۶ دستاورد، ۶ آمار |
| **خرج کردنِ معنادار پاداش** | تم‌ها با ستاره، راهنما با سکه (قیمت روی دکمه) |
| **ریتمِ «یک مرحلهٔ دیگر»** | پایان مرحله بدون معطلی: «مرحلهٔ بعد» درشت و در مرکز |

> نکتهٔ اخلاقی: پالسِ دکمه‌ها عمداً ملایم است و جایزهٔ روزانه با دو روز مهلت/توضیح شفاف پیاده شده تا حس فشار و شرمندگی نسازد.

---

## ۵) برای تست روی گوشی
1. خروجی Actions (Run 6، شاخهٔ main) → Artifact `VazheSafar-apk` یا Release.
2. حجم APK همچنان نزدیک ۵۵–۵۸ مگابایت؛ نسخه ۱٫۱٫۰ (code 3).
3. چیزهایی که مخصوصاً نگاه کن: اندازهٔ دکمه‌ها روی گوشی کوچک، خوانایی نوار پایین، حس نرمی صداها، و اینکه گربه/پیرمرد درست روی میز نشسته باشند.
4. `devtools/Screenshot.gd` برای گرفتن اسکرین‌شات هر صفحه: `xvfb-run -a godot --path game --resolution 1080x1920 --script devtools/Screenshot.gd`
5. `devtools/AudioPreview.gd` برای ریختن موسیقی/جلوه‌ها به WAV.

## ۶) پیشنهاد برای گام بعدی
- **محتوا:** تولید ۱۰۰ مرحلهٔ جدید با مراحل ۶ و ۷ حرفی + پاک‌سازی اسم‌های خاص (اسکریپت مولد از دست رفته را بازمی‌نویسم).
- **گرافیک مرحلهٔ بعد:** بک‌گراند مخصوص هر مرحله (۱۰ دنیا × پس‌زمینه)، انیمیشن ورود کاراکتر در گیم‌پلی، انیمیشن پس‌زمینهٔ پارالاکس.
- **قابلیت‌های نگه‌داشت:** چالش روزانهٔ زمان‌دار، رویداد آخر هفته، جدول امتیاز محلی.
- **انتشار:** آمادهٔ‌سازی AAB + اسکرین‌شات‌های کافه‌بازار + متن توضیحات + سیاست حریم خصوصی.
