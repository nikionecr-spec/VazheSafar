# واژه‌سفر — خلاصهٔ وضعیت پروژه (برای ادامهٔ کار)

> این فایل را ایجنت برای هم‌زمانی سریع نوشته است. آخرین بررسی: ۱۴ سپتامبر ۲۰۲۶

## ۱) دسترسی و مخزن
- حساب گیت‌هاب: `nikionecr-spec` — مخزن: `nikionecr-spec/VazheSafar` (عمومی، شاخهٔ `main`)
- کلون محلی: `/home/user/VazheSafar`
- آخرین کامیت: `988bce6` — «ci: grant contents:write so releases can be published»
- Issues / PRها: هیچ‌کدام باز نیست.
- Releaseها: `v1.0.1` (VazheSafar.apk ≈ ۵۷ مگابایت) و `v1.0.0`
- Actions: آخرین دو اجرا موفق؛ تنها یک اجرای ناموفق (run 3) که فقط در مرحلهٔ *Publish GitHub Release* رد شد (دلیل: نبود مجوز `contents: write`) — با کامیت `988bce6` حل شده.
- Artifactهای قابل دانلود در Actions موجود است (`VazheSafar-apk`).
- ⚠️ توکن دسترسی داخل URL ریموت ذخیره شده؛ بهتر است بعد از این جلسه Rotate شود.

## ۲) معماری
Godot 4.3 · GL Compatibility · GDScript · پرترهٔ ۱۰۸۰×۱۹۲۰ (stretch: canvas_items/expand) · `rendering/root_node_layout_direction=1` (RTL)
`export_filter=all_resources` + `exclude_filter="devtools/*, tools/*"` → پوشهٔ devtools در APK نمی‌رود.

```
game/
├── project.godot        autoload: Game, Audio, Transition
├── export_presets.cfg   پکیج ir.vazhesafar.game · minSdk 21 / target 34 · arm64+arm32 · فقط مجوز لرزش
├── data/levels.json     ۱۰ دنیا + ۱۰۰ مرحله + دیکشنری جایزه (۱۰۴۲ واژه)
├── assets/              3 پس‌زمینه، ۲ کاراکتر، لوگو، ۱۵+ آیکون، فونت وزیرمتن (Black/Bold/Medium)
├── scenes/              ۱۰ صحنه (هر کدام یک ریشه که اسکریپت UI را در کد می‌سازد)
├── scripts/             ۱۸ فایل / ~۴۳۰۰ خط
└── devtools/            PlayTest.gd (۶۰ تست)، Screenshot.gd، ShotPlay.gd، Debug.gd
```

### نقش کلیدی اسکریپت‌ها
| فایل | نقش |
|---|---|
| `Game.gd` (autoload) | دادهٔ مراحل، اقتصاد (سکه/XP/ستاره)، کوئست، جایزهٔ روزانه، سیو در `user://savegame.json`، ارقام فارسی |
| `Audio.gd` (autoload) | سنتز کامل صدا در زمان اجرا: pluck سبک کارپلاس-استرانگ (سنتی/تار)، زنگ، لوپ موسیقی ۲۴ ثانیه‌ای در دستگاه شور، لرزش هپتیک |
| `Transition.gd` (autoload) | گذار صحنه با shader آیریس دایره‌ای |
| `LetterWheel.gd` | چرخ حروف، ورودی swipe، مسیر طلایی با glow، تکسچر کاشی‌ها در زمان اجرا تولید می‌شود |
| `LevelScene.gd` | گیم‌پلی: تختهٔ جدول کلمات، تشخیص کلمه/کلمهٔ جایزه/غلط، راهنما (۸۰ سکه)، بُر، ستاره، پاپ‌آپ برد |
| `UIKit.gd` | زبان بصری مشترک (پنل چوبی/کرم، دکمه‌های براق، توست) |
| `JuicyButton.gd` / `IconButton.gd` | دکمهٔ سه‌بعدی با squash/stretch، shine، ذرات، صدا و لرزش |
| `HomeScene / MapScene / ShopScene / QuestsScene / LibraryScene / ThemesScene / SettingsScene` | صفحه‌ها |

## ۳) وضعیت محتوا (بررسی دادهٔ levels.json)
- ۱۰۰ مرحله، همه از نظر ساخت‌پذیری از حروف چرخ **سالم** (۰ خطا) و بدون کلمهٔ تکراری.
- توزیع حروف: ۴ حرفی = ۲۰ مرحله، ۵ حرفی = ۸۰ مرحله. **هیچ مرحلهٔ ۶ یا ۷ حرفی وجود ندارد** (برخلاف چیزی که README وعده داده).
- کلمات هر مرحله: ۴ کلمه (۵۴ مرحله)، ۵ کلمه (۳۴)، ۶ کلمه (۱۲) → مجموع ۴۵۸ پاسخ.
- دیکشنری جایزه: ۱۰۴۲ واژهٔ ۳ تا ۷ حرفی.
- چند واژهٔ مشکوک/اسم خاص در پاسخ‌ها: «دبی، روم، شیلی، سعد، سما، زید، نجوم، درویش‌وار…» → نیاز به پاک‌سازی برای حس فارسی طبیعی‌تر.
- `game/tools/gen_levels.py` که در README معرفی شده **در مخزن نیست** (مولد مراحل گم شده) → باید بازنویسی و کامیت شود تا مراحل قابل تولید مجدد باشند.

## ۴) قابل بهبودهای شناسایی‌شده
1. **محتوا:** افزودن مراحل ۶ و ۷ حرفی، رساندن به ۲۰۰ مرحله، بازتولید levels.json با اسکریپت نسخه‌شده.
2. **کیفیت واژه:** پاک‌سازی اسم‌های خاص و واژه‌های کم‌کاربرد از پاسخ‌ها و دیکشنری.
3. **کارایی:** ذرات CPU (انفجار کلمه، هالهٔ گرهٔ فعلی، confetti) و بازسازی تخته در هر مرحله؛ پروفایل روی گوشی ضعیف.
4. **UX:** نمایش کلمهٔ در دست ساخت در وسط چرخ حروف، انیمیشن‌های بیشتر برای برد/باخت، راهنمای اول‌بار (tutorial).
5. **صدا:** مسیر صوتی حرفه‌ای‌تر، تنظیم بلندی، امکان خاموشی سریع در HUD.
6. **انتشار:** صفحهٔ کافه‌بازار/مایکت، اسکرین‌شات‌های ۹:۱۶ استاندارد، سیاست حریم خصوصی، نسخهٔ ۶۴ بیتی و AAB.
7. **مهندسی:** اجرای تست‌ها به‌صورت محلی (نیاز به دانلود Godot headless)، افزودن تست‌های UI جدید برای مراحل تازه.

## ۵) برای اجرای محلی (در این سندباکس)
Godot نصب نیست؛ در صورت نیاز باید `Godot_v4.3-stable_linux.x86_64` + `export_templates` دانلود شود (JDK 17 هم لازم است؛ فعلاً JDK 11 موجود است).
اجرای تست: `xvfb-run -a godot --path game --script devtools/PlayTest.gd`
